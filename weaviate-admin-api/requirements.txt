fastapi==0.104.1
uvicorn[standard]==0.24.0
weaviate-client==3.25.3
PyJWT==2.8.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
email-validator==2.2.0

# ── Optional vector-DB providers ─────────────────────────────────────────────
# Uncomment / install the client(s) for the provider(s) you want to use.
# Weaviate is included above and active by default (DB_PROVIDER=weaviate).
#
# Qdrant  — set DB_PROVIDER=qdrant in .env
qdrant-client>=1.7.0
#
# ChromaDB — set DB_PROVIDER=chroma in .env
chromadb>=0.4.0
#
# FAISS — set DB_PROVIDER=faiss in .env  (local, no server needed)
# faiss-cpu>=1.7.4   # CPU-only  — most users
# faiss-gpu>=1.7.4   # GPU/CUDA  — uncomment if you have CUDA

# ── AI (optional) — install only the provider(s) you use ────────────────────
# Set AI_PROVIDER in .env to: groq | openai | anthropic | ollama
#
# Groq  — free tier, recommended default
groq>=1.0.0
#
# OpenAI  — pip install openai
openai>=1.0.0
#
# Anthropic  — pip install anthropic
anthropic>=0.25.0
#
# Ollama — no pip package needed; uses HTTP directly (ollama.com)

